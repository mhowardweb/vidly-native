import React from "react";
import {FlatList, Text, View} from 'react-native';

const ListGroup = ({
  items,
  textProperty,
  valueProperty,
  onItemSelect,
  selectedItem
 }) => (<Text>List</Text>)

export {ListGroup};
